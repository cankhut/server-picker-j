namespace ServerPickerX.Models
{
    // Ordering applied to the server card grid, set by the sort chips
    public enum ServerSortField
    {
        Location,
        Ping,
        PacketLoss
    }
}
